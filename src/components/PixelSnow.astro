<script>
    const snow = document.getElementById('pixel-snow')
    const parallax = (el) => () => {
        const scrollPercent = window.scrollY / (document.documentElement.scrollHeight - document.documentElement.clientHeight)
        el.style.top = `-${50 * scrollPercent}vh`
    }

    document.addEventListener('scroll', parallax(snow))
</script>

<div id="pixel-snow" class="pixel-snow" aria-hidden="true">
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
        <rect x="10" y="3" width="20" height="20" fill="var(--skin-logo-00)" />
        <rect x="100" y="8" width="6" height="6" fill="var(--skin-logo-10)" />
        <rect x="112" y="20" width="17" height="17" fill="var(--skin-logo-30)" />
        <rect x="-6" y="32" width="12" height="12" fill="var(--skin-logo-11)" />
        <rect x="17" y="50" width="17" height="17" fill="var(--skin-logo-21)" />
        <rect x="92" y="44" width="9" height="9" fill="var(--skin-logo-22)" />
        <rect x="110" y="58" width="3" height="3" fill="var(--skin-logo-32)" />    
        <rect x="102" y="70" width="24" height="24" fill="var(--skin-logo-03)" />
        <rect x="13" y="75" width="7" height="7" fill="var(--skin-logo-13)" />
        <rect x="-4" y="89" width="13" height="13" fill="var(--skin-logo-33)" />
    </svg>
</div>

<style>
    .pixel-snow {
        position: fixed;
        top: 0;
        left: 50%;
        transform: translateX(-50%);
        min-width: 100vw;
        width: 150vh;
        height: 150vh;

        z-index: -1;
        opacity: 0.25;
    }

    @media (prefers-reduced-motion) {
        .pixel-snow {
            top: 0 !important;
        }
    }
</style>